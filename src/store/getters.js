export const files = state => state.files

export const stateAudios = state => state.files.audio

export const stateVideos = state => state.files.video

export const stateImages = state => state.files.image

export const isLoggedIn = state => state.isLoggedIn

export const isAdmin = state => state.isAdmin
